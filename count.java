public class count {
    public static void main(String[]args){
        String word = "Ebrainbrain";
        char search = 'a';
       int count=0;
       int length=0;
        for(int i=0;i<word.length();i++)
        {
            if(search == word.charAt(i))
            count++;
        }
        {
          else if (search == word.length(i));
                count++;
        }
        System.out.println("count of " +  search + " is " + count);
        System.out.println("length of " +  search + " is " + count);


    }
}
